# Aksaraku API — Python dependencies (pinned minor versions)
# Update secara rutin: pip install -U <package> lalu jalankan test suite sebelum deploy.

# --- Web Framework ---
fastapi>=0.110.0,<1.0
uvicorn[standard]>=0.29.0,<1.0

# --- Data Validation ---
pydantic>=2.7.0,<3.0

# --- Supabase / Database ---
supabase>=2.3.0,<3.0

# --- AI Providers ---
google-genai>=2.23.0,<3.0        # Official Google GenAI SDK modern (v1)
google-generativeai>=0.8.0,<1.0  # Legacy package untuk fallback
tenacity>=9.1.0,<10.0
openai>=1.30.0,<2.0          # Dipakai untuk Groq + OpenRouter (OpenAI-compatible)

# --- HTTP (dipakai httpx oleh supabase & httpx internal FastAPI) ---
httpx>=0.27.0,<1.0

# --- Rate Limiting ---
slowapi>=0.1.9,<1.0

# --- Environment ---
python-dotenv>=1.0.0,<2.0

# --- Test (tidak diinstall di production) ---
# pytest>=8.0.0
# pytest-asyncio>=0.23.0
